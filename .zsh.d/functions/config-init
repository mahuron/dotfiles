# -*- mode: sh; -*-
config-init() {
    cgit='git --git-dir=${CONFIGDIR} --work-tree=${HOME}'
    if [ ! -d ${CONFIGDIR} ]; then
	git init --bare ${CONFIGDIR}
	eval ${cgit} config --local status.showUntrackedFiles no
    fi
    alias config=${cgit}
}
