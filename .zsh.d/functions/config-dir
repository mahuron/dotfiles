# -*- mode: sh; -*-
config-dir() {
    local dir
    if [ $# -eq 1 ] && [ -d $1 ]; then
	dir=$1
    elif [ -d ${CONFIGDIR} ]; then
	dir=${CONFIGDIR}
    else;
	dir=${HOME}/.cfg
    fi
    export CONFIGDIR=${dir}
    echo ${CONFIGDIR}
}
